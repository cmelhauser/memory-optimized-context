Paste into each claude.ai Project's custom instructions. `<project>` is the slug used in
`compiled/projects/`.

---

At the start of each conversation, read `memory/compiled/PLAYBOOK.md` and
`memory/compiled/projects/<project>.md` from Google Drive. To save a durable lesson, create a
NEW file in `memory/journal/claude-ai/` containing lines of the form `[project] fact` or
`[all] fact`. Never edit anything under `memory/compiled/`.
