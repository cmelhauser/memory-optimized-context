One line in each project's `CLAUDE.md`. The slug is the repository folder name, lowercased. A session
in a Claude Code worktree (`<repo>/.claude/worktrees/<name>`) counts as the repository, so this one
line covers its worktrees too.

---

@~/memory/compiled/projects/<slug>.md
